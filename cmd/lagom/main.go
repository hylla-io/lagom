// Command lagom is the entrypoint for the lagom CLI.
//
// Bootstrap skeleton — replace with the real implementation once lagom's
// domain is decided. Present so `mage build` has an entrypoint to compile and
// the canonical magefile's cmd/*/main.go glob resolves.
package main

import "fmt"

func main() {
	fmt.Println("lagom: bootstrap skeleton — not yet implemented")
}
